<%*
let url = await tp.system.prompt("Enter image URL");
tR += `<figure>
  <img src="https://cdn.jsdelivr.net/gh/pushkar-gooljar/kyber-crystal/_images/${url}" class="custom-img">
  <figcaption>First ionisation energies of period 3 elements</figcaption>
</figure>`;
%>
